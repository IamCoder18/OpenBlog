# pnpm configuration
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
# Point pnpm's content-addressable store at the BuildKit cache mount in the
# Dockerfile so the store survives across `docker build` invocations.
store-dir=/pnpm/store
